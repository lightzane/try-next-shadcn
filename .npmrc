# Refuse to install if the current Node.js or pnpm version does not satisfy
# the "engines" field in package.json. Prevents silent failures from running
# on an unsupported runtime.
engine-strict=true
